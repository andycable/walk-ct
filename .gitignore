cache/
__pycache__/
Commit.txt
file_changes.txt

# PyInstaller build output for convert.fit_to_gpx (44MB, regenerated on build)
src/convert/build/

# Scratch files left behind by one-off runs, not repo content
src/extraction.log
src/sorted_data.tmp
temp_read_parquet.R

# OSM road data fetched by find_unreachable_squadrats.py (35MB, re-fetchable
# from Overpass and rebuilt region by region if deleted)
src/plotmap/road_network_cache.json
